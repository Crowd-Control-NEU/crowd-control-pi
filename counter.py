class Counter:
    def __init__(self, timeControl = 1):
        self.count = 0
        self.timeControl = timeControl
    def update(readingsQueue):
        currentItem = readingsQueue(0)
        currentItemIndex = 0;
        index = 1;
        while readingsQueue not empty:
            nextItem = readingsQueue(index)
            if nextItem(1) != currentItem(1):
                firstOutsideSpot = item.time 
            if item.channel == inside and inside == null:
                firstInsideSpot = item.time
            if firstInsideSpot && firstOutsideSpot
                if firstInsideSpot.time - firstOutsideSpot.time < timeControl
                    count++
                firstInsideSpot = ''
                firstOutsideSpot = ''
                readingsQueue.remove(currentItem)
                currentItem = readingsQueue(currentItemIndex++)
           index++